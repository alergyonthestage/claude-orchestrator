# User configuration — unified structure (created by cco init)
/user-config/

# Legacy layout (pre-migration) — keep during transition
/global/
/projects/

# Secrets
.env
.env.*
user-config/global/secrets.env
global/secrets.env
secrets.env
# Allow template secrets.env (tracked tool code)
!templates/project/base/secrets.env

# Claude Code local settings (generated per-session)
.claude/settings.local.json

# macOS
.DS_Store
